{
  "name": "cloud-resume-counter",
  "version": "1.0.0",
  "description": "Cloudflare Worker + KV backend for the portfolio visitor counter",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "lint": "eslint src"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.22.0",
    "eslint": "^10.9.0",
    "vitest": "^4.1.11",
    "wrangler": "^3.0.0"
  }
}
